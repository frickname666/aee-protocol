# AEE Protocol - Environment Variables Template
# ==============================================
# COPY THIS FILE TO .env AND FILL WITH YOUR VALUES
# NEVER COMMIT .env TO VERSION CONTROL!

# 🔐 CRYPTOGRAPHIC SECRET KEY (REQUIRED)
# Generate with: python -c "import base64, secrets; print(base64.b64encode(secrets.token_bytes(32)).decode())"
# Or use: python -c "from aeeprotocol.sdk.client_secure import AEEClientSecure; print(AEEClientSecure.generate_key())"
AEE_SECRET_KEY='YOUR-BASE64-ENCODED-32-BYTE-KEY-HERE'

# 🎯 WATERMARKING CONFIGURATION (OPTIONAL)
AEE_STRENGTH=0.5
AEE_TARGET_FPR=0.02

# 👤 USER IDENTIFICATION (OPTIONAL)
# This is PUBLIC and can be in version control
AEE_USER_ID=123456

# 📊 DEBUG SETTINGS (OPTIONAL)
AEE_DEBUG=false
AEE_LOG_LEVEL=INFO

# ==============================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Edit .env with your real values
# 3. Add .env to .gitignore
# 4. Never share your AEE_SECRET_KEY!
# ==============================================